<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>burpcat</title>
  <subtitle>development ; philosophy ; poetry</subtitle>
  <link href="https://burpcat.github.io/feed.xml" rel="self"/>
  <link href="https://burpcat.github.io/"/>
  <updated>2026-08-18T00:00:00.000Z</updated>
  <id>https://burpcat.github.io/</id>
  <author>
    <name>Avinash Arutla</name>
    <email>avarutla@gmail.com</email>
  </author>
  
  <entry>
    <title>running away from pip, and how it changed my life</title>
    <link href="https://burpcat.github.io/blog/bye-pip/"/>
    <updated>2026-08-18T00:00:00.000Z</updated>
    <id>https://burpcat.github.io/blog/bye-pip/</id>
    <summary>the experience of moving on</summary>
    <content type="html"><![CDATA[<p>I told it no.</p>
<p>That was the first thing I did when uv was suggested to me. Not “let me look into it.” Just no.</p>
<hr>
<h2 id="the-story" tabindex="-1">The Story <a class="header-anchor" href="#the-story" aria-hidden="true">#</a></h2>
<h3 id="why-pip" tabindex="-1">Why pip? <a class="header-anchor" href="#why-pip" aria-hidden="true">#</a></h3>
<p>Because what else was there.</p>
<p>I was twelve. It was 2012. My first command was <code>pip install requests</code> — not <code>pip3</code>. That one missing character dates me more than anything else I could tell you. Back then, plain <code>pip</code> meant Python 2, and nobody had to specify.</p>
<p>pip came with Python. That was reason enough. I did not choose it any more than I chose the alphabet.</p>
<h3 id="the-thing-i-hated-before-i-needed-it" tabindex="-1">The thing I hated before I needed it <a class="header-anchor" href="#the-thing-i-hated-before-i-needed-it" aria-hidden="true">#</a></h3>
<p>I did not know requirements.txt existed. When I found out, it shook me.</p>
<p>Then I hated it.</p>
<p>Some of that is just how I’m built. I hate configs. And I have the worse problem too — the moment I know something <em>is</em> configurable, I have to go all the way in. So the safest move was to never learn. Same story with virtual environments. I resented setting them up until the day I stopped noticing I was doing it.</p>
<p>But if I’m being honest, that wasn’t the real reason I gave in.</p>
<p>I gave in because I was tired of running a file and being told the thing it needed wasn’t there. Over and over. requirements.txt fixed that, and I only learned the word “reproducibility” later, after the fact, as a justification for something laziness had already decided.</p>
<p>That is the whole pattern, and it repeats. I don’t adopt tools because they’re correct. I adopt them when staying lazy gets harder than switching.</p>
<h3 id="how-i-found-uv" tabindex="-1">How I found uv <a class="header-anchor" href="#how-i-found-uv" aria-hidden="true">#</a></h3>
<p>Claude told me to install it. I said no.</p>
<p>There wasn’t a reason. I was starting a project and did not want a new tool in the middle of it. Then a moment later I thought, why not, and installed it anyway. That is the entire origin story.</p>
<p>Setup gave me a little trouble. Not the commands — those were fine. What bothered me was <code>pyproject.toml</code> sitting there in the project root. I like things organised. Dangling config files in a root directory make me itch, and now there was another one.</p>
<p>Which, if you’re keeping score, is the third time I resisted a tool for reasons that had nothing to do with whether it worked.</p>
<p>It’s fine now. I stopped seeing the file.</p>
<h3 id="why-i-resisted" tabindex="-1">Why I resisted <a class="header-anchor" href="#why-i-resisted" aria-hidden="true">#</a></h3>
<p>The hype didn’t bother me much. Sometimes things are hyped and good. Sometimes they’re hyped and hollow. You can’t tell from the noise, so I don’t read much into it either way.</p>
<p>What scared me was that uv was new.</p>
<p>When I commit to something, I commit. I go deep or I don’t go at all. So the question was never “will this break my project.” It was “am I about to pour myself into a tool that won’t be here in a year.” That’s an expensive mistake for someone who can’t do things halfway.</p>
<p>New tools ask you to spend before you know. I did not want to spend.</p>
<h3 id="why-i-love-it-now" tabindex="-1">Why I love it now <a class="header-anchor" href="#why-i-love-it-now" aria-hidden="true">#</a></h3>
<p>Three things turned me, and they arrived in order.</p>
<p>First, I cloned a project and it just came up. One command. No venv to create, no install step, no fiddling. The gap between having the code and running the code closed to nothing.</p>
<p>Second, I watched <code>uv add</code> install a package, record it, and update the lockfile in one move. Three things I used to do by hand, in sequence, and forget one of. I had been the glue between those steps for a decade without noticing I was doing a job.</p>
<p>Third — and this is the one that actually mattered — I stopped thinking about environments. I didn’t decide to. I noticed it later, the way you notice a noise only after it stops.</p>
<p>That’s the atomicity I was after. Not speed. I never cared much about speed. I wanted one thing to be responsible for the whole state of my project, so I could stop holding it together myself.</p>
<h3 id="the-pattern" tabindex="-1">The pattern <a class="header-anchor" href="#the-pattern" aria-hidden="true">#</a></h3>
<p>Write it out and it’s embarrassing.</p>
<p>I resisted requirements.txt because I didn’t want to learn a config. I resisted virtual environments because they were tedious. I resisted uv because a file looked wrong in my project root, and because I was afraid to spend myself on something unproven.</p>
<p>Not one of those was a technical objection. Not one.</p>
<p>And every time, the tool won the same way. Not by convincing me. By making the lazy path and the correct path the same path. I was tired of <code>ModuleNotFoundError</code>, so requirements.txt won. I was tired of being the glue between three commands, so uv won.</p>
<p>I’m not a careful engineer who evaluates tools on merit. I’m a lazy one who eventually gets tired enough. The tools that have stuck are the ones that were patient with that.</p>
<hr>
<h2 id="coming-from-pip-the-two-doors-into-uv" tabindex="-1">Coming from pip: the two doors into uv <a class="header-anchor" href="#coming-from-pip-the-two-doors-into-uv" aria-hidden="true">#</a></h2>
<p>Most migration guides tell you to put <code>uv</code> in front of your pip commands. That works. It is also the least interesting thing uv does, and it hid the real tool from me for about two months.</p>
<p>uv has two separate interfaces. They look similar and they are not the same. <code>uv pip</code> operates directly on an environment, the way pip always did. The project commands — <code>uv add</code>, <code>uv sync</code>, <code>uv run</code> — operate on a project, defined by a <code>pyproject.toml</code>. One is a faster pip. The other replaces the whole habit.</p>
<p>I took the first door because it cost nothing. I stayed there too long. This section walks both, in order, and says what each one costs.</p>
<hr>
<h3 id="door-one-uv-as-a-faster-pip" tabindex="-1">Door One: uv as a faster pip <a class="header-anchor" href="#door-one-uv-as-a-faster-pip" aria-hidden="true">#</a></h3>
<p>The mapping is almost boring. That is the point.</p>
<table>
<thead>
<tr>
<th>pip / pip-tools / virtualenv</th>
<th>uv</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>python -m venv .venv</code></td>
<td><code>uv venv</code></td>
</tr>
<tr>
<td><code>pip install requests</code></td>
<td><code>uv pip install requests</code></td>
</tr>
<tr>
<td><code>pip install -r requirements.txt</code></td>
<td><code>uv pip install -r requirements.txt</code></td>
</tr>
<tr>
<td><code>pip freeze &gt; requirements.txt</code></td>
<td><code>uv pip freeze &gt; requirements.txt</code></td>
</tr>
<tr>
<td><code>pip-compile requirements.in</code></td>
<td><code>uv pip compile requirements.in -o requirements.txt</code></td>
</tr>
<tr>
<td><code>pip-sync requirements.txt</code></td>
<td><code>uv pip sync requirements.txt</code></td>
</tr>
</tbody>
</table>
<p><em>Same verbs, same flags, new engine underneath.</em></p>
<p>Install uv once. It does not need Python to run, which is the first sign it is not a normal Python tool:</p>
<pre class="language-bash"><code class="language-bash">$ <span class="token function">curl</span> <span class="token parameter variable">-LsSf</span> https://astral.sh/uv/install.sh <span class="token operator">|</span> <span class="token function">sh</span></code></pre>
<p>Make an environment:</p>
<pre class="language-bash"><code class="language-bash">$ uv venv
Using CPython <span class="token number">3.12</span>.3 interpreter at: /usr/bin/python3.12
Creating virtual environment at: .venv
Activate with: <span class="token builtin class-name">source</span> .venv/bin/activate</code></pre>
<p><em>uv tells you which interpreter it picked. pip’s venv never did. Small thing, saves real time later.</em></p>
<p>Install something:</p>
<pre class="language-bash"><code class="language-bash">$ uv pip <span class="token function">install</span> requests
Resolved <span class="token number">5</span> packages <span class="token keyword">in</span> 12ms
Installed <span class="token number">5</span> packages <span class="token keyword">in</span> 45ms
 + <span class="token assign-left variable">certifi</span><span class="token operator">==</span><span class="token number">2024.7</span>.4
 + charset-normalizer<span class="token operator">==</span><span class="token number">3.3</span>.2
 + <span class="token assign-left variable">idna</span><span class="token operator">==</span><span class="token number">3.7</span>
 + <span class="token assign-left variable">requests</span><span class="token operator">==</span><span class="token number">2.32</span>.3
 + <span class="token assign-left variable">urllib3</span><span class="token operator">==</span><span class="token number">2.2</span>.2</code></pre>
<p>Look at the two numbers on those first lines. <strong>Resolve</strong> and <strong>install</strong> are separate steps, and uv reports them separately. Resolving is the hard part — working out which versions of which packages can coexist. pip does this too, it just never showed you, and it was never fast enough to be worth showing.</p>
<p>That split matters for an intuition you will need later: the expensive thinking and the cheap copying are different operations, and uv is built to do the thinking once and reuse it.</p>
<hr>
<h3 id="what-door-one-quietly-gives-up" tabindex="-1">What Door One quietly gives up <a class="header-anchor" href="#what-door-one-quietly-gives-up" aria-hidden="true">#</a></h3>
<p>This is where most posts stop. It shouldn’t be.</p>
<p><strong>Your pip config stops being read.</strong> uv deliberately ignores <code>pip.conf</code> and <code>PIP_*</code> environment variables. It does not want bug-for-bug compatibility with pip, so it keeps its own configuration hierarchy instead. If your team pipes installs through a private index configured in <code>pip.conf</code>, that setting silently does nothing under <code>uv pip</code>. You configure it again, in uv’s own config.</p>
<p><strong>Not every pip flag exists.</strong> uv supports a large subset of pip’s options and subcommands, not all of them. Missing pieces get prioritized by user demand. Common workflows are fine. The further you get from common workflows, the more likely you are to hit a difference.</p>
<p><strong>And the deeper cost: nothing structural changed.</strong> You still have a <code>requirements.txt</code> that you maintain by hand. You still have to remember to activate the venv. You still get “works on my machine,” just faster. Door One buys you speed and nothing else.</p>
<p>I ran like this for weeks and told people I had switched to uv. I hadn’t.</p>
<hr>
<h3 id="door-two-the-project-model" tabindex="-1">Door Two: the project model <a class="header-anchor" href="#door-two-the-project-model" aria-hidden="true">#</a></h3>
<p>The shift is in what your project’s <em>source of truth</em> is.</p>
<p>Under pip, <code>requirements.txt</code> is a flat list of packages, and it tries to do two jobs at once: declare what you depend on, and pin exactly what got installed. Those jobs conflict. Loosen the pins and builds drift. Tighten them and you can never tell which packages you actually asked for versus which came along as baggage.</p>
<p>uv splits them into two files:</p>
<ul>
<li><strong><code>pyproject.toml</code></strong> — what you <em>want</em>. Your direct dependencies, loosely constrained. Human-written.</li>
<li><strong><code>uv.lock</code></strong> — what you <em>got</em>. Every package in the tree, exact versions. Machine-written, never edited by hand.</li>
</ul>
<p>The lockfile is what replaces requirements.txt. <code>pyproject.toml</code> replaces the part of your brain that was trying to remember why <code>urllib3</code> was in the file.</p>
<p><strong>Bringing an existing project across takes two commands.</strong></p>
<pre class="language-bash"><code class="language-bash">$ uv init <span class="token parameter variable">--bare</span>
$ uv <span class="token function">add</span> <span class="token parameter variable">-r</span> requirements.txt
Resolved <span class="token number">5</span> packages <span class="token keyword">in</span> 8ms
Installed <span class="token number">5</span> packages <span class="token keyword">in</span> 40ms</code></pre>
<p><em><code>--bare</code> creates just the pyproject.toml — no sample code, no README you did not ask for.</em></p>
<p>One behaviour to expect: <code>uv add</code> strips the pins on the way in. <code>requests==2.31.0</code> in your old file becomes plain <code>requests</code> in pyproject.toml, and the exact version gets captured in uv.lock instead. That looks like data loss the first time you see it. It isn’t. It is the split doing its job — the constraint lives in one file, the pin lives in the other. If you genuinely need a hard pin declared, pass it explicitly: <code>uv add 'requests==2.31.0'</code>.</p>
<p>Dev-only packages come in through their own door and land in a separate group:</p>
<pre class="language-bash"><code class="language-bash">$ uv <span class="token function">add</span> <span class="token parameter variable">--dev</span> <span class="token parameter variable">-r</span> requirements-dev.txt</code></pre>
<p><strong>Then three commands cover most days:</strong></p>
<pre class="language-bash"><code class="language-bash">$ uv <span class="token function">add</span> pandas        <span class="token comment"># install, record in pyproject.toml, re-lock</span>
$ uv remove pandas     <span class="token comment"># uninstall, un-record, re-lock</span>
$ uv run app.py        <span class="token comment"># sync the environment, then run</span></code></pre>
<p>Each of those is one command doing what used to be three, and the third one is the one that changed my habits. Before every <code>uv run</code>, uv checks that the lockfile matches pyproject.toml and that the environment matches the lockfile. I stopped activating virtual environments. Not out of discipline — I just stopped needing to.</p>
<hr>
<h3 id="anatomy-of-a-pyprojecttoml" tabindex="-1">Anatomy of a pyproject.toml <a class="header-anchor" href="#anatomy-of-a-pyprojecttoml" aria-hidden="true">#</a></h3>
<p>Worth reading field by field, because you will hand-edit this file eventually.</p>
<pre class="language-toml"><code class="language-toml"><span class="token punctuation">[</span><span class="token table class-name">project</span><span class="token punctuation">]</span>
<span class="token key property">name</span> <span class="token punctuation">=</span> <span class="token string">"my-project"</span>
<span class="token key property">version</span> <span class="token punctuation">=</span> <span class="token string">"0.1.0"</span>
<span class="token key property">description</span> <span class="token punctuation">=</span> <span class="token string">"Add your description here"</span>
<span class="token key property">readme</span> <span class="token punctuation">=</span> <span class="token string">"README.md"</span>
<span class="token key property">requires-python</span> <span class="token punctuation">=</span> <span class="token string">">=3.12"</span>
<span class="token key property">dependencies</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span>
    <span class="token string">"requests>=2.32.3"</span><span class="token punctuation">,</span>
    <span class="token string">"pandas>=2.2.0"</span><span class="token punctuation">,</span>
<span class="token punctuation">]</span>

<span class="token punctuation">[</span><span class="token table class-name">dependency-groups</span><span class="token punctuation">]</span>
<span class="token key property">dev</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span>
    <span class="token string">"pytest>=8.0.0"</span><span class="token punctuation">,</span>
    <span class="token string">"ruff>=0.5.0"</span><span class="token punctuation">,</span>
<span class="token punctuation">]</span>

<span class="token punctuation">[</span><span class="token table class-name">build-system</span><span class="token punctuation">]</span>
<span class="token key property">requires</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span><span class="token string">"hatchling"</span><span class="token punctuation">]</span>
<span class="token key property">build-backend</span> <span class="token punctuation">=</span> <span class="token string">"hatchling.build"</span></code></pre>
<p><strong><code>requires-python</code></strong> declares the versions your project supports. It is a range, and it is the constraint uv resolves against.</p>
<p><strong><code>dependencies</code></strong> is your direct dependency list. Only what you import. Nothing transitive.</p>
<p><strong><code>[dependency-groups]</code></strong> holds things you need to develop but not to run. The distinction from <code>optional-dependencies</code> matters if you publish: optional-dependencies become part of your package’s public metadata, while dependency-groups stay local to your working copy and are invisible to people who install your package. Test runners, linters, and type checkers go here.</p>
<p><strong><code>[build-system]</code></strong> only matters if you are packaging a library for others to install. Building an application? You can leave it out.</p>
<hr>
<h3 id="the-lockfile-and-the-tradeoff-it-creates" tabindex="-1">The lockfile, and the tradeoff it creates <a class="header-anchor" href="#the-lockfile-and-the-tradeoff-it-creates" aria-hidden="true">#</a></h3>
<p><code>uv.lock</code> is the reason reproducibility works. It is also the sharpest edge in the tool.</p>
<p><strong>Commit it.</strong> Then everyone who clones your repo resolves to identical versions.</p>
<p><strong>Know that it auto-relocks.</strong> In development, uv notices a stale lockfile and quietly re-resolves it for you. Convenient at your desk. In CI, that silent update defeats the entire point of having a lockfile. Two flags exist for this:</p>
<pre class="language-bash"><code class="language-bash">$ uv <span class="token function">sync</span> <span class="token parameter variable">--locked</span>    <span class="token comment"># fail the build if uv.lock is out of date</span>
$ uv <span class="token function">sync</span> <span class="token parameter variable">--frozen</span>    <span class="token comment"># install uv.lock as-is, skip checking pyproject.toml</span></code></pre>
<p><em><code>--locked</code> asserts the lockfile is current. <code>--frozen</code> asserts nothing and just installs what’s there. Use <code>--locked</code> when you want drift to break the build.</em></p>
<p><strong>And the real tradeoff: <code>uv.lock</code> is not a portable format.</strong> The format is specific to uv, which is what lets it support features requirements.txt cannot express. Anything downstream that expects a requirements file — an old Docker base image, a deploy target you do not control, a colleague who has not switched — cannot read it. There is an escape hatch: uv exports the lockfile to requirements.txt, PEP 751’s pylock.toml, or a CycloneDX SBOM.</p>
<pre class="language-bash"><code class="language-bash">$ uv <span class="token builtin class-name">export</span> <span class="token parameter variable">--format</span> requirements-txt --no-dev <span class="token operator">></span> requirements.txt</code></pre>
<p><em>The bridge back out. Useful in Docker builds, and useful the week you have to prove to someone that nothing is locked in.</em></p>
<p><strong>One more sharp edge.</strong> Hand-editing a dependency in <code>pyproject.toml</code> does not update <code>uv.lock</code>. uv detects the mismatch and refuses to run, and the error message is not always obvious about why. Early users hit this hard enough to <a href="https://github.com/astral-sh/uv/issues/9923">file it as a footgun</a>. The habit that fixes it: after any manual edit to pyproject.toml, run <code>uv lock</code>. <code>uv add</code> and <code>uv remove</code> do it for you.</p>
<hr>
<h3 id="layer-three-python-versions-and-tools" tabindex="-1">Layer Three: Python versions and tools <a class="header-anchor" href="#layer-three-python-versions-and-tools" aria-hidden="true">#</a></h3>
<p>This is the part with no pip equivalent at all, and it is where “uv replaced five tools” stops being marketing.</p>
<p><strong>Python versions</strong> — what pyenv did:</p>
<pre class="language-bash"><code class="language-bash">$ uv python <span class="token function">install</span> <span class="token number">3.12</span>
$ uv python pin <span class="token number">3.12</span>
Pinned <span class="token variable"><span class="token variable">`</span>.python-version<span class="token variable">`</span></span> to <span class="token variable"><span class="token variable">`</span><span class="token number">3.12</span><span class="token variable">`</span></span></code></pre>
<p>uv reads <code>.python-version</code> on every command, searching the working directory and each parent. Commit the file. Your team lands on the same interpreter without anyone being told to.</p>
<p>One constraint to hold in your head: <code>.python-version</code> pins your development interpreter, <code>requires-python</code> in pyproject.toml declares the supported range, and the pin has to satisfy the range. Break that and uv reports an incompatibility rather than guessing. Two files, two jobs — the same split as pyproject.toml and uv.lock.</p>
<p><strong>Global CLI tools</strong> — what pipx did:</p>
<pre class="language-bash"><code class="language-bash">$ uv tool <span class="token function">install</span> ruff
Resolved <span class="token number">1</span> package <span class="token keyword">in</span> 6ms
Installed <span class="token number">1</span> package <span class="token keyword">in</span> 2ms
 + <span class="token assign-left variable">ruff</span><span class="token operator">==</span><span class="token number">0.5</span>.4
Installed <span class="token number">1</span> executable: ruff</code></pre>
<p><strong>Or skip installing entirely.</strong> <code>uvx</code> is an alias for <code>uv tool run</code>. It builds a temporary isolated environment, runs the tool, and throws the environment away.</p>
<pre class="language-bash"><code class="language-bash">$ uvx pycowsay <span class="token string">'hello uv'</span>
Resolved <span class="token number">1</span> package <span class="token keyword">in</span> 167ms
Installed <span class="token number">1</span> package <span class="token keyword">in</span> 9ms
 + <span class="token assign-left variable">pycowsay</span><span class="token operator">==</span><span class="token number">0.0</span>.0.2
------------
<span class="token operator">&lt;</span> hello uv <span class="token operator">></span>
------------
 <span class="token punctuation">\</span>   ^__^
  <span class="token punctuation">\</span>  <span class="token punctuation">(</span>oo<span class="token punctuation">)</span><span class="token punctuation">\</span>_______
     <span class="token punctuation">(</span>__<span class="token punctuation">)</span><span class="token punctuation">\</span>       <span class="token punctuation">)</span><span class="token punctuation">\</span>/<span class="token punctuation">\</span>
         <span class="token operator">||</span>----w <span class="token operator">|</span>
         <span class="token operator">||</span>     <span class="token operator">||</span></code></pre>
<p><em>First run pays for the download. Cached runs come back in tens of milliseconds.</em></p>
<p><strong>The tradeoff here is isolation, and it bites.</strong> A tool run through <code>uvx</code> cannot see your project’s packages. If the tool needs them — pytest and mypy both do — use <code>uv run</code> instead, or it runs against an environment that knows nothing about your code. The rule: does the code belong to your project, or is it a standalone tool? Project code gets <code>uv run</code>. Standalone tools get <code>uvx</code>.</p>
<hr>
<h3 id="which-door-to-walk-through" tabindex="-1">Which door to walk through <a class="header-anchor" href="#which-door-to-walk-through" aria-hidden="true">#</a></h3>
<p>Both, in this order. That is not a hedge, it is a migration path.</p>
<p><strong>Start at Door One</strong> if you have a working project and a deadline. <code>uv pip install</code> is a one-line change to your muscle memory and your CI scripts. You get the speed immediately and risk almost nothing. Just know you have changed your installer and nothing else.</p>
<p><strong>Move to Door Two</strong> when the thing hurting you is not speed. If your problems sound like “it worked yesterday” or “it works on my machine” or “why is this package even in here” — those are structure problems. A faster installer cannot fix them. The lockfile can.</p>
<p><strong>Stay at Door One</strong> in one real case: legacy projects and situations where you need direct manual control over an environment, without a pyproject.toml in the picture. Similarly, a few tools call <code>pip</code> directly — Jupyter’s <code>%pip</code> magic, for one — and those need actual pip present in the environment. The project model assumes your project is the unit of work. When it isn’t, don’t force it.</p>
<p>I went through Door One for the speed and stayed for weeks. Door Two is the one that changed how I work.</p>
<hr>
<h2 id="where-uv-stops-being-obvious" tabindex="-1">Where uv stops being obvious <a class="header-anchor" href="#where-uv-stops-being-obvious" aria-hidden="true">#</a></h2>
<p>Section 2’s tradeoffs were about the tool’s own design. These are about the places uv meets systems it does not control: a container build, a CI runner, a scientific stack held together by C libraries, a repo with eight packages in it.</p>
<p>Two of the three turn out to be configuration problems. One is a wall, and no amount of configuration moves it.</p>
<hr>
<h3 id="containers-speed-makes-layering-matter-more-not-less" tabindex="-1">Containers: speed makes layering matter more, not less <a class="header-anchor" href="#containers-speed-makes-layering-matter-more-not-less" aria-hidden="true">#</a></h3>
<p>The intuition that trips people up: uv is so fast that Docker layering feels unnecessary. It is the opposite. When installs take 40ms, the <em>download</em> and the <em>layer invalidation</em> become the whole cost. Getting the layer order wrong now dominates your build time in a way it never did with pip, because there is nothing else left to blame.</p>
<p><strong>Split the dependency install from the project install.</strong> <code>uv sync --no-install-project</code> installs your dependencies but not your project. Dependencies change rarely; your code changes constantly. Separating them means editing a source file does not re-resolve your dependency tree.</p>
<pre class="language-dockerfile"><code class="language-dockerfile"><span class="token instruction"><span class="token keyword">FROM</span> python:3.12-slim</span>
<span class="token instruction"><span class="token keyword">COPY</span> <span class="token options"><span class="token property">--from</span><span class="token punctuation">=</span><span class="token string">ghcr.io/astral-sh/uv:latest</span></span> /uv /uvx /bin/</span>

<span class="token instruction"><span class="token keyword">WORKDIR</span> /app</span>

<span class="token comment"># Dependencies only — cached until uv.lock changes</span>
<span class="token instruction"><span class="token keyword">RUN</span> <span class="token options"><span class="token property">--mount</span><span class="token punctuation">=</span><span class="token string">type=cache,target=/root/.cache/uv</span> <span class="token operator">\</span>
    <span class="token property">--mount</span><span class="token punctuation">=</span><span class="token string">type=bind,source=uv.lock,target=uv.lock</span> <span class="token operator">\</span>
    <span class="token property">--mount</span><span class="token punctuation">=</span><span class="token string">type=bind,source=pyproject.toml,target=pyproject.toml</span></span> <span class="token operator">\</span>
    uv sync --locked --no-install-project</span>

<span class="token comment"># Now the project, which changes on every commit</span>
<span class="token instruction"><span class="token keyword">COPY</span> . /app</span>
<span class="token instruction"><span class="token keyword">RUN</span> <span class="token options"><span class="token property">--mount</span><span class="token punctuation">=</span><span class="token string">type=cache,target=/root/.cache/uv</span></span> <span class="token operator">\</span>
    uv sync --locked</span></code></pre>
<p><em>The bind mounts matter: pyproject.toml is needed to identify the project root and name, but the project contents stay out of the image until the final sync.</em></p>
<p><strong>Three settings you will otherwise discover through warnings:</strong></p>
<p><code>UV_LINK_MODE=copy</code> — uv hardlinks files by default. A Docker cache mount and your sync target sit on different filesystems, where hardlinks do not work. Without this, uv warns on every build.</p>
<p><code>.venv</code> in <code>.dockerignore</code> — the project virtual environment depends on your local platform. It should be built fresh inside the image, never copied in.</p>
<p><code>UV_COMPILE_BYTECODE=1</code> — precompiles bytecode, which improves startup time at the cost of longer installs and a bigger image. A production tradeoff, not a default. Choose deliberately.</p>
<p><strong>The workspace footgun.</strong> In a workspace, that first layered sync has to use <code>--frozen</code>, not <code>--locked</code>. uv cannot verify the lockfile is current without every member’s pyproject.toml, and those have not been copied in yet. Use <code>--no-install-workspace</code> to exclude all members, then <code>--locked</code> on the second sync once everything is present.</p>
<p><strong>And to ship an image without uv in it:</strong> <code>--no-editable</code> installs the project in non-editable mode, which drops the dependency on your source directory. That lets a multi-stage build copy just the virtual environment into the final image and leave the source behind.</p>
<hr>
<h3 id="ci-the-cache-you-should-not-keep" tabindex="-1">CI: the cache you should not keep <a class="header-anchor" href="#ci-the-cache-you-should-not-keep" aria-hidden="true">#</a></h3>
<p>This is the most counterintuitive result I found in the whole tool, and it is worth internalizing because it inverts a habit everyone has.</p>
<p>Standard CI advice: cache your dependencies. With uv, it is often <em>faster</em> to leave pre-built wheels out of the cache and re-download them from the registry every run.</p>
<p>Why: restoring a cache is I/O, and uv’s download-plus-install path is fast enough to beat it. The cache stops being a shortcut and becomes overhead. uv ships a command for exactly this — <code>uv cache prune --ci</code> strips pre-built wheels and unzipped source distributions while keeping wheels that had to be <em>built</em> from source, since those are genuinely expensive to reproduce.</p>
<p>Astral recommends running it at the end of the CI job. Worth noting the nuance: at least one third-party action leaves that pruning off by default, citing Charlie Marsh’s own research. The honest read is that the right answer depends on your dependency mix — how much of your tree needs compiling. Measure it rather than copying a config.</p>
<p>A working baseline:</p>
<pre class="language-yaml"><code class="language-yaml"><span class="token key atrule">jobs</span><span class="token punctuation">:</span>
  <span class="token key atrule">test</span><span class="token punctuation">:</span>
    <span class="token key atrule">runs-on</span><span class="token punctuation">:</span> ubuntu<span class="token punctuation">-</span>latest
    <span class="token key atrule">steps</span><span class="token punctuation">:</span>
      <span class="token punctuation">-</span> <span class="token key atrule">uses</span><span class="token punctuation">:</span> actions/checkout@v6
      <span class="token punctuation">-</span> <span class="token key atrule">uses</span><span class="token punctuation">:</span> astral<span class="token punctuation">-</span>sh/setup<span class="token punctuation">-</span>uv@v8.1.0
        <span class="token key atrule">with</span><span class="token punctuation">:</span>
          <span class="token key atrule">enable-cache</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>
      <span class="token punctuation">-</span> <span class="token key atrule">run</span><span class="token punctuation">:</span> uv sync <span class="token punctuation">-</span><span class="token punctuation">-</span>locked <span class="token punctuation">-</span><span class="token punctuation">-</span>all<span class="token punctuation">-</span>extras <span class="token punctuation">-</span><span class="token punctuation">-</span>dev
      <span class="token punctuation">-</span> <span class="token key atrule">run</span><span class="token punctuation">:</span> uv run pytest</code></pre>
<p><em><code>--locked</code> is the point of this file. It fails the build if uv.lock is out of date, instead of silently re-resolving.</em></p>
<p><strong>Two sharp edges:</strong></p>
<p><code>actions/checkout</code> must run <em>before</em> <code>setup-uv</code>. The reverse order is not supported.</p>
<p>The first run on a new cache key prints “No GitHub Actions cache found.” This is expected, not a misconfiguration — the action cannot distinguish a legitimate first run from a broken <code>cache-dependency-glob</code>, so it warns either way.</p>
<hr>
<h3 id="the-wall-conda-and-native-dependencies" tabindex="-1">The wall: conda and native dependencies <a class="header-anchor" href="#the-wall-conda-and-native-dependencies" aria-hidden="true">#</a></h3>
<p>Everything above is tuning. This part is not, and I think it is where the “uv replaces five tools” framing does real damage — because it sets up an expectation the tool cannot meet.</p>
<p><strong>uv installs Python packages from PyPI. That is the entire scope.</strong> It cannot install the CUDA toolkit, GDAL, or HDF5. It cannot install compilers, or share an environment with R or Julia.</p>
<p>The reason is structural, not a missing feature. A conda package is not required to be Python at all — it can be a shared library, a compiler, or a command-line tool like ffmpeg. The format is just “a binary for this platform with these dependencies,” and conda’s solver reasons across languages because it knows about non-Python deps. A wheel, however much compiled C it contains, is still shaped around a Python entry point.</p>
<p>The concrete case: a CUDA build of PyTorch needs a specific CUDA runtime. conda installs both together, atomically. With PyPI, you install CUDA system-wide yourself and hope the wheel matches.</p>
<p>There is also no import path. Unlike conda-to-pixi, uv has no <code>--import</code> flag for environment.yml, because uv reads PyPI and conda reads channels. Migration means re-declaring your dependencies by hand.</p>
<p><strong>So the decision is about where your packages live, not which tool is better:</strong></p>
<table>
<thead>
<tr>
<th>Your dependencies</th>
<th>Tool</th>
</tr>
</thead>
<tbody>
<tr>
<td>All on PyPI as wheels — NumPy, pandas, scikit-learn, SciPy</td>
<td>uv</td>
</tr>
<tr>
<td>Native libs from conda-forge — CUDA, GDAL, HDF5, compilers</td>
<td>conda family</td>
</tr>
<tr>
<td>Both</td>
<td>conda or pixi for the native foundation, uv for the Python layer</td>
</tr>
</tbody>
</table>
<p><em>These are not competitors. Each is best at a specific kind of dependency.</em></p>
<p>The interesting middle option is <strong>pixi</strong>. It resolves conda-forge and PyPI packages together into a single lockfile — and it uses uv internally for the PyPI half. Project environments live in a local <code>.pixi/</code> directory, with no global <code>conda activate</code> step. If you want uv’s project model but need conda’s package universe, that is the tool, and it is under-mentioned in every uv post I have read.</p>
<p><strong>Two smaller caveats worth knowing, though attribute these loosely</strong> — they come from practitioner reports rather than official docs. uv has not shipped a 1.0, which some corporate procurement processes gate on. Its offline behaviour is reportedly not fully mature, which matters in air-gapped networks. And the cache grows: one user reported over 20GB after a year, clearable instantly with <code>uv cache clean</code>, but worth watching on disk-constrained runners.</p>
<hr>
<h3 id="workspaces-one-lockfile-one-python-shared-blame" tabindex="-1">Workspaces: one lockfile, one Python, shared blame <a class="header-anchor" href="#workspaces-one-lockfile-one-python-shared-blame" aria-hidden="true">#</a></h3>
<p>uv borrows workspaces from Cargo: a collection of packages managed together, each with its own pyproject.toml, all sharing a single lockfile.</p>
<pre class="language-toml"><code class="language-toml"><span class="token punctuation">[</span><span class="token table class-name">project</span><span class="token punctuation">]</span>
<span class="token key property">name</span> <span class="token punctuation">=</span> <span class="token string">"albatross"</span>
<span class="token key property">dependencies</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span><span class="token string">"bird-feeder"</span><span class="token punctuation">,</span> <span class="token string">"tqdm>=4,&lt;5"</span><span class="token punctuation">]</span>

<span class="token punctuation">[</span><span class="token table class-name">tool.uv.sources</span><span class="token punctuation">]</span>
<span class="token key property">bird-feeder</span> <span class="token punctuation">=</span> <span class="token punctuation">{</span> <span class="token key property">workspace</span> <span class="token punctuation">=</span> <span class="token boolean">true</span> <span class="token punctuation">}</span>

<span class="token punctuation">[</span><span class="token table class-name">tool.uv.workspace</span><span class="token punctuation">]</span>
<span class="token key property">members</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span><span class="token string">"packages/*"</span><span class="token punctuation">]</span>
<span class="token key property">exclude</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span><span class="token string">"packages/seeds"</span><span class="token punctuation">]</span></code></pre>
<p><em><code>workspace = true</code> tells uv to resolve bird-feeder from the workspace instead of PyPI.</em></p>
<p><code>uv lock</code> operates on the whole workspace at once. <code>uv run</code> and <code>uv sync</code> default to the root, and both take <code>--package</code> to target a member from any directory.</p>
<p>The shared lockfile is the feature. Every package in the repo resolves against one consistent set of versions. It is also the source of all three tradeoffs.</p>
<p><strong>One <code>requires-python</code> for everyone.</strong> uv enforces a single Python requirement across the workspace — the intersection of every member’s <code>requires-python</code>. If you need to test one member on a Python version the rest of the workspace does not support, you fall back to <code>uv pip</code> in a separate virtual environment. The shared lockfile cannot express two answers.</p>
<p><strong>Conflicting requirements break the model.</strong> Workspaces are not suited to members with conflicting dependencies, or members that each want their own virtual environment. The alternative is path dependencies — declare each package independently and point at it with <code>tool.uv.sources</code>. You keep most of the benefit and lose <code>uv run --package</code>; commands must then run from the relevant directory.</p>
<p><strong>And uv cannot enforce the boundaries it helps you draw.</strong> Python provides no dependency isolation, so uv cannot stop one workspace member from importing a dependency declared by another. Your package boundaries are a convention the tool records but cannot police. An accidental cross-import works fine locally and fails the day you publish the package alone.</p>
<p><strong>When it earns its keep:</strong> a root application with accompanying libraries, a library with a performance-critical extension module in Rust or C++, or a plugin system where each plugin depends on the root. uv itself is built this way — core library and CLI as separate packages, so each can be tested without the other.</p>
<hr>
<h3 id="the-shape-of-it" tabindex="-1">The shape of it <a class="header-anchor" href="#the-shape-of-it" aria-hidden="true">#</a></h3>
<p>Three areas, three different kinds of limit.</p>
<p>Containers and CI are <strong>configuration problems.</strong> They have right answers, the right answers are non-obvious, and you find them by reading docs rather than by intuition. The CI cache result in particular inverts standard practice.</p>
<p>Workspaces are a <strong>design problem.</strong> One lockfile buys consistency and costs flexibility. That trade is inherent, not a bug, and if your packages genuinely disagree about their dependencies you should not be in a workspace.</p>
<p>conda is a <strong>scope problem</strong>, and the only one where the answer is “use something else.” uv is a Python package manager that got very good at being a Python package manager. It did not become a system package manager, and it is not trying to.</p>
<p>None of this argues against uv. It argues against the framing that one binary replaced everything — which was the thing that made me suspicious of uv in the first place, and turned out to be the only part of my skepticism that held up.</p>
<hr>
<h2 id="my-life-now" tabindex="-1">My life now <a class="header-anchor" href="#my-life-now" aria-hidden="true">#</a></h2>
<blockquote>
<p>who pip?</p>
</blockquote>
]]></content>
  </entry>
  
  <entry>
    <title>What drives quality agentic code?</title>
    <link href="https://burpcat.github.io/blog/spec-v-feat/"/>
    <updated>2026-07-24T00:00:00.000Z</updated>
    <id>https://burpcat.github.io/blog/spec-v-feat/</id>
    <summary>I built two projects a few weeks apart, one spec-first and one feature-first. The labels inverted, and neither predicted where the work paid off. Verifiability did.</summary>
    <content type="html"><![CDATA[<p><em>I built two projects a few weeks apart, one spec-first and one feature-first. The labels inverted, and neither predicted where the work paid off. Verifiability did.</em></p>
<p><code>shrutz</code> is a macOS wallpaper rotator that advances on active input time instead of the wall clock: about 2,300 lines of bash daemon plus a Swift menu-bar app. <code>slap</code> is a cold-outreach CLI over the GMass API: about 7,165 lines of Python across 19 modules, 796 test functions, a local dashboard. I built the first spec-driven and the second feature-driven. That was the plan.</p>
<h2 id="the-labels-inverted" tabindex="-1">The labels inverted <a class="header-anchor" href="#the-labels-inverted" aria-hidden="true">#</a></h2>
<p><code>slap</code>, the feature-driven one, opened against a 468-line numbered build brief (sections 0 to 14, ending in a literal Build Order) with a read-only review pass gating every commit from hour one. <code>shrutz</code>, the spec-driven one, spent two weeks as direct-to-master commits titled things like <code>FULL POWER</code>; its only spec-like document was written <em>after</em> the engine, weather, gallery, and JSON API had already shipped.</p>
<pre class="language-mermaid"><code class="language-mermaid"><span class="token keyword">gantt</span>
    dateFormat YYYY-MM-DD
    title Both projects drifted off their labels
    section shrutz <span class="token text string">(spec-driven?)</span>
    Organic, no spec, direct to master    <span class="token operator">:</span>2026-06-05, 18d
    Worktrees, PRs, visual audit loop      <span class="token operator">:</span>2026-07-14, 10d
    section slap <span class="token text string">(feature-driven?)</span>
    468-line brief, review before commit   <span class="token operator">:</span>2026-07-02, 12d
    Branch + PR per feature                <span class="token operator">:</span>2026-07-14, 10d</code></pre>
<p>So the label decided little. One question decided a lot: can this result be verified any other way?</p>
<pre class="language-mermaid"><code class="language-mermaid"><span class="token keyword">flowchart</span> TD
    A<span class="token text string">[New surface to build]</span> <span class="token arrow operator">--></span> B<span class="token text string">{Can a normal test&lt;br/>verify the result?}</span>
    B <span class="token arrow operator">--></span><span class="token label property">|Yes|</span> C<span class="token text string">[Iterate. Tests + review suffice.&lt;br/>A spec just adds docs to keep in sync.]</span>
    B <span class="token arrow operator">--></span><span class="token label property">|No|</span> D<span class="token text string">[Build the verification harness FIRST:&lt;br/>screenshot audit, API probe, fault injection]</span>
    D <span class="token arrow operator">--></span> E<span class="token text string">[Then build the feature against it]</span></code></pre>
<p>Both projects, independently, aimed their heaviest upfront rigor at the surfaces where the answer was <em>no</em>.</p>
<h2 id="rigor-concentrated-on-the-unverifiable-surfaces" tabindex="-1">Rigor concentrated on the unverifiable surfaces <a class="header-anchor" href="#rigor-concentrated-on-the-unverifiable-surfaces" aria-hidden="true">#</a></h2>
<p>In <code>shrutz</code> that surface was the rendered UI. The repo’s only real spec-conformance loop drove the menu-bar app’s visual design: approved mockups, a live screenshot captured from the window server, then a written audit diffing pixel dimensions and source constants against the reference. It caught a class of bug tests structurally cannot. Version 2.5 shipped a redesign that still rendered as a flat, dusty taupe. Two compounding color-math bugs: the wallpaper sampler averaged raw RGB per region (which collapses toward grey when saturated and desaturated pixels mix), and five ambient blobs composited with ordinary alpha blending under <code>.ultraThinMaterial</code> (which regresses further toward grey on overlap). The fix was saturation-weighted HSB extraction with <em>circular</em> hue averaging:</p>
<pre class="language-swift"><code class="language-swift"><span class="token comment">// simplified: circular mean hue, so reds near 0°/360° don't average to cyan</span>
<span class="token keyword">func</span> <span class="token function-definition function">meanHue</span><span class="token punctuation">(</span><span class="token omit keyword">_</span> hues<span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token class-name">Double</span><span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token operator">-></span> <span class="token class-name">Double</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> x <span class="token operator">=</span> hues<span class="token punctuation">.</span>map <span class="token punctuation">{</span> <span class="token function">cos</span><span class="token punctuation">(</span><span class="token short-argument">$0</span> <span class="token operator">*</span> <span class="token punctuation">.</span>pi <span class="token operator">/</span> <span class="token number">180</span><span class="token punctuation">)</span> <span class="token punctuation">}</span><span class="token punctuation">.</span><span class="token function">reduce</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token operator">+</span><span class="token punctuation">)</span>
    <span class="token keyword">let</span> y <span class="token operator">=</span> hues<span class="token punctuation">.</span>map <span class="token punctuation">{</span> <span class="token function">sin</span><span class="token punctuation">(</span><span class="token short-argument">$0</span> <span class="token operator">*</span> <span class="token punctuation">.</span>pi <span class="token operator">/</span> <span class="token number">180</span><span class="token punctuation">)</span> <span class="token punctuation">}</span><span class="token punctuation">.</span><span class="token function">reduce</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token operator">+</span><span class="token punctuation">)</span>
    <span class="token keyword">return</span> <span class="token function">atan2</span><span class="token punctuation">(</span>y<span class="token punctuation">,</span> x<span class="token punctuation">)</span> <span class="token operator">*</span> <span class="token number">180</span> <span class="token operator">/</span> <span class="token punctuation">.</span>pi   <span class="token comment">// naive (350 + 10) / 2 = 180° (cyan); this gives ~0° (red)</span>
<span class="token punctuation">}</span>
<span class="token comment">// plus compositing: .compositingGroup().blendMode(.plusLighter) then .saturation(1.3)</span></code></pre>
<p>In <code>slap</code> the unverifiable surface was a third party’s live behavior. Before any dependent code existed, probe scripts hit the real GMass API and found the docs wrong three ways:</p>
<pre class="language-text"><code class="language-text"># probes/, run 2026-07-02, pinned in CONTROL_SHEET.md
stop-on-reply : per-stage (stageOneAction..stageEightAction), NOT one global flag
send endpoint : POST /api/campaigns/{campaignDraftId}   # ID in path; body-field form -> HTTP 400
attachments   : JSON {fileName, contentType, base64Content} only  # multipart -> HTTP 415</code></pre>
<p>The same sheet records what can <em>never</em> be verified: stop-on-reply is write-only, no read-back, so a 200 on send is the strongest signal obtainable. Naming that limit explicitly is the spec doing its actual job.</p>
<p>Everywhere the result <em>was</em> testable, both codebases grew by accretion. <code>slap</code>’s core files show churn ratios near 1.0 (nearly every line ever written is still present) and zero reverts in the entire history.</p>
<h2 id="where-it-broke-invariants-across-call-sites" tabindex="-1">Where it broke: invariants across call sites <a class="header-anchor" href="#where-it-broke-invariants-across-call-sites" aria-hidden="true">#</a></h2>
<p>GMass rewrites <code>&lt;a href&gt;</code> targets for click tracking. A plain-text body has no anchor to rewrite, so <code>clickTracking: true</code> is inert. I fixed it once in the draft path by converting to minimal HTML. Three weeks later the identical root cause resurfaced in the follow-up-message path, a different function carrying the same invariant that never got the conversion.</p>
<p><code>shrutz</code> hit the same shape as a naming drift: the installer created the set at <code>wallpapers/hassan/</code> but seeded state with <code>ACTIVE_SET=default</code>, so the daemon searched a directory that never existed, and <code>launchd</code>’s <code>KeepAlive</code> turned that into a crash loop on every fresh install. The fix removes the ability to drift by deriving both from one source:</p>
<pre class="language-diff"><code class="language-diff"><span class="token unchanged"><span class="token prefix unchanged"> </span>WALLS_DEFAULT="$LIB/wallpapers/hassan"
</span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span>ACTIVE_SET_DEFAULT="$(basename "$WALLS_DEFAULT")"   # state's ACTIVE_SET can't diverge from the dir we create
</span><span class="token unchanged"><span class="token prefix unchanged"> </span>...
</span><span class="token deleted-sign deleted"><span class="token prefix deleted">-</span>    printf 'CURRENT_INDEX=0\nACTIVE_SECONDS=0\nACTIVE_SET=default\n'      > "$LIB/state"
</span><span class="token inserted-sign inserted"><span class="token prefix inserted">+</span>    printf 'CURRENT_INDEX=0\nACTIVE_SECONDS=0\nACTIVE_SET=%s\n' "$ACTIVE_SET_DEFAULT" > "$LIB/state"
</span></code></pre>
<p>Diff-scoped review, human or automated, is blind to a rule that must hold across call sites: the defect isn’t <em>in</em> the diff. The durable fix is structural. One source of truth, or a written invariant with a grep over every site that touches it.</p>
<h2 id="where-it-broke-atomicity" tabindex="-1">Where it broke: atomicity <a class="header-anchor" href="#where-it-broke-atomicity" aria-hidden="true">#</a></h2>
<p><code>slap</code> needed a new value in a SQLite <code>CHECK</code> constraint, which means rebuilding the table. The first attempt used <code>executescript()</code>, which force-commits any pending transaction before it runs, <em>including the <code>BEGIN</code> the same function just opened</em>. A crash between the rename and the drop would permanently strand the append-only event log, the app’s single source of truth.</p>
<pre class="language-python"><code class="language-python"><span class="token keyword">def</span> <span class="token function">_migrate_events_check_constraint</span><span class="token punctuation">(</span>conn<span class="token punctuation">:</span> sqlite3<span class="token punctuation">.</span>Connection<span class="token punctuation">)</span> <span class="token operator">-</span><span class="token operator">></span> <span class="token boolean">None</span><span class="token punctuation">:</span>
    row <span class="token operator">=</span> conn<span class="token punctuation">.</span>execute<span class="token punctuation">(</span>
        <span class="token string">"SELECT sql FROM sqlite_master WHERE type='table' AND name='events'"</span>
    <span class="token punctuation">)</span><span class="token punctuation">.</span>fetchone<span class="token punctuation">(</span><span class="token punctuation">)</span>
    <span class="token keyword">if</span> row <span class="token keyword">is</span> <span class="token boolean">None</span> <span class="token keyword">or</span> <span class="token string">"'stopped'"</span> <span class="token keyword">in</span> row<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">:</span>
        <span class="token keyword">return</span>  <span class="token comment"># fresh db, or already migrated</span>
    conn<span class="token punctuation">.</span>execute<span class="token punctuation">(</span><span class="token string">"BEGIN"</span><span class="token punctuation">)</span>
    <span class="token keyword">try</span><span class="token punctuation">:</span>
        conn<span class="token punctuation">.</span>execute<span class="token punctuation">(</span><span class="token string">"ALTER TABLE events RENAME TO events_pre_stopped_migration"</span><span class="token punctuation">)</span>
        conn<span class="token punctuation">.</span>execute<span class="token punctuation">(</span>_EVENTS_TABLE_SQL<span class="token punctuation">)</span>          <span class="token comment"># execute(), NEVER executescript()</span>
        conn<span class="token punctuation">.</span>execute<span class="token punctuation">(</span><span class="token string">"INSERT INTO events (...) SELECT ... FROM events_pre_stopped_migration"</span><span class="token punctuation">)</span>
        conn<span class="token punctuation">.</span>execute<span class="token punctuation">(</span><span class="token string">"DROP TABLE events_pre_stopped_migration"</span><span class="token punctuation">)</span>
        conn<span class="token punctuation">.</span>commit<span class="token punctuation">(</span><span class="token punctuation">)</span>
    <span class="token keyword">except</span> BaseException<span class="token punctuation">:</span>
        conn<span class="token punctuation">.</span>rollback<span class="token punctuation">(</span><span class="token punctuation">)</span>
        <span class="token keyword">raise</span></code></pre>
<p>The regression test (simplified) injects a crash right after the rename and asserts the DB is left in its original, unmigrated state:</p>
<pre class="language-python"><code class="language-python"><span class="token keyword">class</span> <span class="token class-name">_FlakyConnection</span><span class="token punctuation">(</span>sqlite3<span class="token punctuation">.</span>Connection<span class="token punctuation">)</span><span class="token punctuation">:</span>
    _armed <span class="token operator">=</span> <span class="token boolean">False</span>
    <span class="token keyword">def</span> <span class="token function">execute</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> sql<span class="token punctuation">,</span> <span class="token operator">*</span>a<span class="token punctuation">)</span><span class="token punctuation">:</span>
        <span class="token keyword">if</span> sql<span class="token punctuation">.</span>startswith<span class="token punctuation">(</span><span class="token string">"ALTER TABLE events RENAME"</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
            self<span class="token punctuation">.</span>_armed <span class="token operator">=</span> <span class="token boolean">True</span>                       <span class="token comment"># arm on the rename</span>
        <span class="token keyword">elif</span> self<span class="token punctuation">.</span>_armed<span class="token punctuation">:</span>
            <span class="token keyword">raise</span> sqlite3<span class="token punctuation">.</span>OperationalError<span class="token punctuation">(</span><span class="token string">"injected crash"</span><span class="token punctuation">)</span>  <span class="token comment"># blow up on the next statement</span>
        <span class="token keyword">return</span> <span class="token builtin">super</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span>execute<span class="token punctuation">(</span>sql<span class="token punctuation">,</span> <span class="token operator">*</span>a<span class="token punctuation">)</span>

<span class="token keyword">def</span> <span class="token function">test_migration_rolls_back_cleanly_on_a_mid_migration_failure</span><span class="token punctuation">(</span>tmp_path<span class="token punctuation">)</span><span class="token punctuation">:</span>
    conn <span class="token operator">=</span> sqlite3<span class="token punctuation">.</span>connect<span class="token punctuation">(</span>tmp_path <span class="token operator">/</span> <span class="token string">"e.db"</span><span class="token punctuation">,</span> factory<span class="token operator">=</span>_FlakyConnection<span class="token punctuation">)</span>
    <span class="token comment"># ...seed the old schema, run the migration, expect the raise...</span>
    <span class="token comment"># assert the live `events` table is intact and still unmigrated</span></code></pre>
<p>That is a correctness <em>guarantee</em>, not a happy-path check, and it exists only because the migration was reviewed before it shipped, not after it corrupted something.</p>
<h2 id="where-it-broke-the-platform-underneath" tabindex="-1">Where it broke: the platform underneath <a class="header-anchor" href="#where-it-broke-the-platform-underneath" aria-hidden="true">#</a></h2>
<p><code>shrutz</code>’s daemon blocked on a foreground <code>sleep</code>. Bash defers trap handling until the foreground command returns, so <code>next</code>, <code>pause</code>, and <code>resume</code> could take up to <code>CHECK_EVERY</code> seconds (30 by default) to apply. Backgrounding the sleep and <code>wait</code>-ing on it lets a signal interrupt immediately:</p>
<pre class="language-bash"><code class="language-bash"><span class="token comment"># before: SIGUSR1 is queued until sleep returns (up to CHECK_EVERY seconds later)</span>
<span class="token keyword">while</span> <span class="token builtin class-name">:</span><span class="token punctuation">;</span> <span class="token keyword">do</span> tick<span class="token punctuation">;</span> <span class="token function">sleep</span> <span class="token string">"<span class="token variable">$CHECK_EVERY</span>"</span><span class="token punctuation">;</span> <span class="token keyword">done</span>

<span class="token comment"># after: wait is interruptible, so the trap fires the instant the signal lands</span>
<span class="token builtin class-name">trap</span> <span class="token string">'apply_now'</span> USR1
<span class="token keyword">while</span> <span class="token builtin class-name">:</span><span class="token punctuation">;</span> <span class="token keyword">do</span>
    tick
    <span class="token function">sleep</span> <span class="token string">"<span class="token variable">$CHECK_EVERY</span>"</span> <span class="token operator">&amp;</span>
    <span class="token function">wait</span> <span class="token string">"<span class="token variable">$!</span>"</span>
<span class="token keyword">done</span></code></pre>
<p>And one fix spawned the next: a Redis lock added around dashboard GMass polling left the old direct-poll path live for the case where Redis was <em>unreachable</em>, so a Redis outage with concurrent dashboard loads could fire multiple uncoordinated GMass sweeps, discovered three days later. Iteration fixes the failure in front of you and leaves the adjacent one running.</p>
<h2 id="the-verifiable-side-is-only-as-good-as-its-adversary" tabindex="-1">The verifiable side is only as good as its adversary <a class="header-anchor" href="#the-verifiable-side-is-only-as-good-as-its-adversary" aria-hidden="true">#</a></h2>
<p>On surfaces you <em>can</em> test, both projects earned trust by testing adversarially rather than happily. <code>shrutz</code>’s gallery installer is proven against a real zip-slip payload:</p>
<pre class="language-python"><code class="language-python"><span class="token comment"># guard, shrutz:1938: resolve every member and reject anything escaping the set dir</span>
dest_real <span class="token operator">=</span> os<span class="token punctuation">.</span>path<span class="token punctuation">.</span>realpath<span class="token punctuation">(</span>dest_dir<span class="token punctuation">)</span>
target    <span class="token operator">=</span> os<span class="token punctuation">.</span>path<span class="token punctuation">.</span>realpath<span class="token punctuation">(</span>os<span class="token punctuation">.</span>path<span class="token punctuation">.</span>join<span class="token punctuation">(</span>dest_real<span class="token punctuation">,</span> rel<span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token keyword">if</span> os<span class="token punctuation">.</span>path<span class="token punctuation">.</span>commonpath<span class="token punctuation">(</span><span class="token punctuation">[</span>dest_real<span class="token punctuation">,</span> target<span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token operator">!=</span> dest_real<span class="token punctuation">:</span>
    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f"error: unsafe path in zip: </span><span class="token interpolation"><span class="token punctuation">{</span>member<span class="token punctuation">}</span></span><span class="token string">"</span></span><span class="token punctuation">,</span> <span class="token builtin">file</span><span class="token operator">=</span>sys<span class="token punctuation">.</span>stderr<span class="token punctuation">)</span><span class="token punctuation">;</span> sys<span class="token punctuation">.</span>exit<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre>
<pre class="language-bash"><code class="language-bash">@test <span class="token string">"gallery install: rejects a zip-slip attempt and writes nothing outside the set dir"</span> <span class="token punctuation">{</span>
    run <span class="token string">"<span class="token variable">$SHRUTZ</span>"</span> gallery <span class="token function">install</span> evilset          <span class="token comment"># zip contains ../../../../tmp/pwned.txt</span>
    <span class="token punctuation">[</span> <span class="token string">"<span class="token variable">$status</span>"</span> <span class="token parameter variable">-ne</span> <span class="token number">0</span> <span class="token punctuation">]</span> <span class="token operator">&amp;&amp;</span> <span class="token punctuation">[</span> <span class="token operator">!</span> <span class="token parameter variable">-f</span> <span class="token string">"/tmp/shrutz-zipslip-pwned.txt"</span> <span class="token punctuation">]</span>
<span class="token punctuation">}</span></code></pre>
<p>Some architecture only exists to keep a surface testable at all. <code>shrutz</code> splits wallpaper application into a correct-but-expensive path and a fast-but-partial one, and the daemon picks per event:</p>
<pre class="language-mermaid"><code class="language-mermaid"><span class="token keyword">flowchart</span> TD
    T<span class="token text string">[Daemon tick / manual skip]</span> <span class="token arrow operator">--></span> Q<span class="token text string">{advance rotation&lt;br/>or cheap correction?}</span>
    Q <span class="token arrow operator">--></span><span class="token label property">|advance|</span> F<span class="token text string">[Rewrite Index.plist:&lt;br/>every Space, per-display override, default]</span>
    F <span class="token arrow operator">--></span> FR<span class="token text string">[Restart wallpaper agent]</span>
    FR <span class="token arrow operator">--></span> FC<span class="token text string">[Fully correct, expensive]</span>
    Q <span class="token arrow operator">--></span><span class="token label property">|per-tick|</span> A<span class="token text string">[AppleScript: what does the&lt;br/>visible Space think it shows?]</span>
    A <span class="token arrow operator">--></span> AC<span class="token text string">{stale?}</span>
    AC <span class="token arrow operator">--></span><span class="token label property">|yes|</span> AF<span class="token text string">[Fix just this Space, instantly]</span>
    AC <span class="token arrow operator">--></span><span class="token label property">|no|</span> AN<span class="token text string">[Do nothing]</span></code></pre>
<h2 id="what-i’m-keeping" tabindex="-1">What I’m keeping <a class="header-anchor" href="#what-i’m-keeping" aria-hidden="true">#</a></h2>
<p>Spend spec effort only where the result resists a normal test: rendered pixels, a third party’s live API, hardware behavior. Build the check <em>before</em> the feature. Everywhere else, tests plus review beat more prose.</p>
<p>Don’t hand-maintain docs; they go stale fastest exactly when the code moves fastest. <code>slap</code>’s context doc still claims a shipped feature isn’t built, and the fix for that staleness sits on a branch that was never merged. <code>shrutz</code> still carries a CI config that auto-merges to master with no review gate, a fossil of a commit titled “removed PR necessity.” Prefer a spec that verifies itself: a screenshot audit, an API probe, a failing test.</p>
<p>Review before the commit, not after. Folding a fix into the commit that introduced the problem is why <code>slap</code>’s rework was only 4.3% of lines changed despite being 17.5% of commits.</p>
<p>Treat cross-call-site invariants as first-class: one source of truth, or a documented rule plus a grep over every site. That, more than any methodology name, is what “spec-driven” should have meant all along.</p>
]]></content>
  </entry>
  
  <entry>
    <title>The pattern repeats</title>
    <link href="https://burpcat.github.io/blog/the-pattern-repeats/"/>
    <updated>2026-06-26T00:00:00.000Z</updated>
    <id>https://burpcat.github.io/blog/the-pattern-repeats/</id>
    <summary>A welcome note, and a small argument that today&#39;s expensive AI is just early compute wearing a new coat.</summary>
    <content type="html"><![CDATA[<p>Welcome. This is where I’ll think out loud.</p>
<p>Here’s the thought I want to start with. Before computers were cheap, <em>computer</em> was a job title — a person, usually a room full of people, doing arithmetic by hand. When the machines arrived they were rare and absurdly expensive, so you didn’t waste them. Time on a mainframe was rationed. You batched your work, handed only the hard problems to the machine, and did the rest yourself. Plenty of smart people looked at that and concluded compute would always be scarce — something you borrowed carefully, never something you spilled.</p>
<p>They were wrong, but not because they misjudged the machine. They were wrong because spending all that money on scarce compute is exactly what bought the abundance that came after. The expensive version had to exist first.</p>
<p>Agents and AI are sitting in that same room right now. Tokens cost money. A capable agent is something you ration, something you hand the hard problems to while you do the cheap parts yourself. It <em>feels</em> like a ceiling. It’s a floor.</p>
<p>So that’s the lens I’ll be writing through — not magic, not hype, just a pattern that’s run before and is running again. If it costs too much or runs too slow today, that’s not the end of the story. It’s the part where someone decides it’s worth paying for anyway.</p>
<p>Glad you’re here. Pull up a chair.</p>
]]></content>
  </entry>
  
</feed>
