# Dev.to personal API key -- generate at https://dev.to/settings/extensions
# Required only for `npm run blog:syndicate`. Copy this file to `.env` and
# fill in your key; `.env` is gitignored so it never gets committed.
DEVTO_API_KEY=
