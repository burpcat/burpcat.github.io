Place the calm-mode background track here as:

    flowers-of-the-forest.mp3

Referenced by #calmAudio in src/_includes/base.njk and copied straight through
to _site/audio/ by the addPassthroughCopy("src/audio") line in .eleventy.js.
This file only exists so the passthrough copy has a directory to point at -- the
actual mp3 is not committed here (it's a commercial recording; licensing it for
site use is the site owner's call).
